diff --git a/.github/workflows/deploy-web.yml b/.github/workflows/deploy-web.yml
index a5f606f..70c42f3 100644
--- a/.github/workflows/deploy-web.yml
+++ b/.github/workflows/deploy-web.yml
@@ -38,6 +38,18 @@ jobs:
       - name: Install dependencies
         run: npm ci
 
+      - name: Install companion dependencies
+        working-directory: .
+        run: npm ci --prefix mobile
+
+      - name: Verify dashboard and mobile
+        working-directory: .
+        run: |
+          npm test
+          npm run test:mobile
+          npx tsc --noEmit
+          cd mobile && npx tsc --noEmit
+
       - name: Build (OpenNext)
         # NEXT_PUBLIC_* must be in scope at build time so Next.js can
         # inline them into the browser bundle. wrangler.toml's [vars]
diff --git a/.github/workflows/eas-update-ota.yml b/.github/workflows/eas-update-ota.yml
index 86c9934..a67fc43 100644
--- a/.github/workflows/eas-update-ota.yml
+++ b/.github/workflows/eas-update-ota.yml
@@ -39,6 +39,18 @@ jobs:
       - name: Install dependencies
         run: npm ci
 
+      - name: Install companion dependencies
+        working-directory: .
+        run: npm ci
+
+      - name: Verify dashboard and mobile
+        working-directory: .
+        run: |
+          npm test
+          npm run test:mobile
+          npx tsc --noEmit
+          cd mobile && npx tsc --noEmit
+
       - name: Setup EAS
         uses: expo/expo-github-action@v8
         with:
